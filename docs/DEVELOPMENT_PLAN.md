# План разработки: AI ROI Calculator

## Текущий статус

**Дата последнего обновления:** 21 февраля 2026  
**Текущая фаза:** Фаза 10 завершена (Авторизация и переделка шаблонов)  
**Прогресс:** Фазы 1–10 завершены. Приложение развёрнуто на VPS Timeweb: http://89.23.112.115

| Фаза | Название | Приоритет | Статус |
|------|----------|-----------|--------|
| 1 | Инициализация проекта | P0 | ✅ Завершена |
| 2 | Модель данных и API | P0 | ✅ Завершена |
| 3 | Калькулятор — ввод данных | P0 | ✅ Завершена |
| 4 | Расчёты и графики | P0 | ✅ Завершена |
| 5 | Библиотека и история | P1 | ✅ Завершена |
| 6 | Админ-панель | P1 | ✅ Завершена (удалена в Фазе 10) |
| 7 | Деплой на Timeweb | P1 | ✅ Завершена |
| 8 | Полировка | P2 | 🔄 Частично |
| 9 | ИИ-генерация данных | P1 | ✅ Завершена |
| 10 | Авторизация и переделка шаблонов | P1 | ✅ Завершена |

---

## Фаза 1: Инициализация проекта

### Задачи
- [x] Создать Next.js 14 проект с TypeScript и App Router
- [x] Настроить Tailwind CSS
- [x] Установить и настроить shadcn/ui (11 компонентов)
- [x] Установить Prisma + SQLite
- [x] Установить Recharts
- [x] Создать базовый layout (header, навигация, контейнер)
- [x] Настроить структуру папок согласно README
- [x] Создать главную страницу (landing)

### Результат
✅ Работающий скелет приложения с главной страницей, навигацией и пустым калькулятором.

### Технические детали
- Использован `shadcn@latest` (v3) с Tailwind CSS v3
- Prisma 6 с генерацией клиента в `src/generated/prisma/`
- Prisma config через `prisma.config.ts` (новый формат v6)
- DATABASE_URL задаётся как абсолютный путь в `.env`

---

## Фаза 2: Модель данных и API

### Задачи
- [x] Описать Prisma schema (12 моделей: расчёты + шаблоны)
- [x] Инициализировать SQLite БД (`npx prisma db push`)
- [x] Реализовать API-роуты:
  - `POST /api/calculations` — создать расчёт (→ redirect на `/{id}`)
  - `GET /api/calculations/:id` — получить расчёт со всеми связями
  - `PUT /api/calculations/:id` — обновить в транзакции (replace all)
  - `DELETE /api/calculations/:id` — удалить с каскадом
  - `GET /api/templates` — список опубликованных шаблонов
  - `GET /api/templates/:id` — получить шаблон
  - `POST /api/templates/:id/use` — создать расчёт из шаблона
  - `GET/POST /api/admin/templates` — с проверкой токена
  - `GET/PUT/DELETE /api/admin/templates/:id` — с проверкой токена
- [x] Создать TypeScript типы в `src/types/index.ts`
- [x] Создать Prisma client singleton в `src/lib/db.ts`

### Результат
✅ Полностью рабочий бэкенд с CRUD для расчётов и шаблонов.

### Технические детали
- Все PUT-операции используют `prisma.$transaction` — атомарный replace всех дочерних записей
- Каскадное удаление через `onDelete: Cascade` на всех FK
- Импорт `PrismaClient` из `@/generated/prisma/client` (Prisma 6 output path)

---

## Фаза 3: Калькулятор — ввод данных

### Задачи
- [x] Создать компонент `CalculatorClient` со stepper/tabs навигацией (8 шагов)
- [x] Шаг 1: Таблица процесса AS-IS (`ProcessTable`)
  - Динамическое добавление/удаление строк
  - Перетаскивание строк (drag & drop) для изменения порядка
  - Автовычисление: стоимость шага, удельное время, удельная стоимость
  - Итоговая строка с суммами
- [x] Шаг 2: Таблица процесса TO-BE
  - Кнопка «Скопировать из AS-IS» (копирует все строки одним кликом)
  - Перетаскивание строк (drag & drop) для изменения порядка
  - Те же вычисления
- [x] Шаг 3: Таблица ошибок AS-IS (`ErrorTable`)
  - Перетаскивание строк (drag & drop) для изменения порядка
- [x] Шаг 4: Таблица ошибок TO-BE
  - Кнопка «Скопировать из AS-IS» (копирует все строки одним кликом)
  - Перетаскивание строк (drag & drop) для изменения порядка
- [x] Шаг 5: Форма динамики раскатки (`RolloutForm`)
  - Выбор модели (LINEAR / S_CURVE / INSTANT) через карточки
  - Поля параметров (операции/мес., целевая доля, срок раскатки)
  - Превью кривой раскатки (LineChart)
- [x] Шаг 6: Таблица CAPEX (`CapexTable`)
- [x] Шаг 7: Таблица OPEX (`OpexTable`)
- [x] Автосохранение (debounced PUT, 500ms) при любом изменении
- [x] Редактируемое название расчёта (inline Input в шапке)
- [x] Индикаторы «Сохранение…» / «Сохранено» (Badge)
- [x] Кнопка «Скопировать ссылку» (navigator.clipboard)
- [x] Сохранение ID в localStorage при первом открытии

### Результат
✅ Полностью рабочий ввод данных по всем шагам калькулятора с автовычислением и автосохранением.

---

## Фаза 4: Расчёты и графики

### Задачи
- [x] Реализовать модуль расчётов в `src/lib/calculations/`:
  - `process-savings.ts` — `computeProcessStep`, `sumProcessSteps`, `calcProcessSavings`, `calcProductivityMultipliers`
  - `error-savings.ts` — `computeErrorItem`, `sumErrorItems`, `calcErrorSavings`
  - `rollout.ts` — `getRolloutShare`, `generateRolloutCurve` (linear / s-curve / instant)
  - `roi.ts` — `calcRoi`: помесячный расчёт, накопленная выгода, точка окупаемости, мультипликаторы производительности
- [x] Шаг 8: Экран результатов (`ResultsPanel`)
  - KPI-карточки: ROI за 12 мес., точка окупаемости, экономия за год, экономия за операцию
  - Блок «Эффект автоматизации»: мультипликаторы роста производительности, снижения стоимости, ускорения процесса
  - Детализация: экономия на процессе vs ошибках за операцию
  - График 1: Динамика окупаемости (AreaChart с заливкой)
  - График 2: Помесячная экономия (ComposedChart: bars + line)
  - График 3: Кривая раскатки (LineChart)
  - График 4: Сравнение AS-IS vs TO-BE (горизонтальный BarChart)
- [x] Выбор горизонта расчёта (12/24/36 месяцев) через Select
- [x] Responsive графики через `ResponsiveContainer`

### Результат
✅ Полный расчёт ROI с четырьмя информативными графиками и ключевыми метриками.

### Технические детали
- S-кривая реализована через логистическую функцию с нормировкой на `targetShare`
- Все вычисления — чистые функции на клиенте, нет обращений к API
- TypeScript strict: типы Tooltip `formatter` приведены к `number | string | undefined`

---

## Фаза 5: Библиотека и история

### Задачи
- [x] Страница «Библиотека шаблонов» (`/templates`) — `TemplatesClient`
  - Карточки с названием, описанием, отраслью и счётчиками этапов
  - Поиск по тексту (Input)
  - Фильтрация по отрасли (pill-кнопки)
  - Кнопка «Использовать шаблон» — POST → redirect на новый расчёт
  - Пустое состояние при отсутствии шаблонов
- [x] Страница «Мои расчёты» (`/my`) — `MyCalculationsClient`
  - Список расчётов из localStorage с загрузкой деталей с сервера
  - Сортировка по дате изменения
  - Удаление из списка (только из localStorage, не из БД)
  - Очистка стale ID при загрузке
  - Пустое состояние с CTA
- [x] Сохранение ID расчётов в localStorage
- [x] Страница 404 (`not-found.tsx`)

### Результат
✅ Пользователь может находить свои прошлые расчёты и использовать готовые шаблоны.

---

## Фаза 6: Админ-панель

### Задачи
- [x] Страница `/admin/{token}` — проверка на сервере через `process.env.ADMIN_SECRET_TOKEN`
- [x] `AdminClient` — список шаблонов с операциями:
  - Публикация / снятие с публикации
  - Редактирование (открывает форму)
  - Удаление с confirm-диалогом
- [x] `AdminTemplateForm` — форма создания/редактирования:
  - Базовые поля: название, описание, отрасль
  - Выбор модели раскатки по умолчанию
  - Редактируемые списки: этапы AS-IS, TO-BE, ошибки AS-IS, TO-BE
  - Статьи CAPEX / OPEX (без сумм — пользователь подставит свои)
- [x] API-роуты для админа с проверкой токена (header `x-admin-token` или query `?token=`)

### Результат
✅ Администратор может управлять библиотекой шаблонов через веб-интерфейс.

---

## Фаза 7: Деплой на Timeweb

### Задачи
- [x] Создать VPS на Timeweb (Ubuntu 22.04, минимальный тариф) — ID 6709061, IP 89.23.112.115
- [x] Установить Node.js 20 LTS (через NodeSource)
- [x] Установить PM2 для управления процессом
- [x] Установить и настроить Nginx
- [x] Клонировать репозиторий на сервер (`/opt/ai-roi-calculator`)
- [x] Настроить `.env` (ADMIN_SECRET_TOKEN, DATABASE_URL с абсолютным путём)
- [x] Сгенерировать Prisma client (`npx prisma generate`)
- [x] Инициализировать БД (`npx prisma db push`)
- [x] Собрать проект (`npm run build`)
- [x] Запустить через PM2 (`pm2 start npm --name roi -- start`)
- [x] Настроить автозапуск PM2 при перезагрузке

### Параметры сервера

| Параметр | Значение |
|----------|----------|
| Провайдер | Timeweb Cloud |
| ID сервера | `6709061` |
| IP | `89.23.112.115` |
| Регион | ru-1 (Санкт-Петербург, зона spb-3) |
| Тариф (preset) | `2449` — 1 vCPU 3.3 GHz / 2 GB RAM / 30 GB NVMe / 1 Gbit |
| ОС | Ubuntu 22.04 LTS |
| Node.js | v20.20.0 (LTS, установлен через NodeSource) |
| PM2 | v6.0.14, процесс `roi` (id: 0) |
| Nginx | 1.18.0 |

### SSH-доступ

SSH-ключ `second-brain` (ID `533441`) привязан к серверу при создании.
Алиас добавлен в `~/.ssh/config` на локальной машине:

```
Host ai-roi-calculator
    HostName 89.23.112.115
    User root
    IdentityFile ~/.ssh/id_ed25519_timeweb
    ServerAliveInterval 60
    ServerAliveCountMax 3
```

```bash
# Подключение:
ssh ai-roi-calculator
```

### Структура на сервере

```
/opt/ai-roi-calculator/     # корень проекта (git clone)
├── .env                    # продакшн-переменные (не в git)
├── deploy.sh               # скрипт обновления
├── prisma/
│   └── prod.db             # SQLite продакшн-база
└── ...                     # остальные файлы проекта
```

### Переменные окружения (`.env` на сервере)

```env
DATABASE_URL="file:/opt/ai-roi-calculator/prisma/prod.db"
ADMIN_SECRET_TOKEN="<секрет хранится только на сервере>"
NODE_ENV="production"
```

Посмотреть токен на сервере:
```bash
ssh ai-roi-calculator "grep ADMIN_SECRET_TOKEN /opt/ai-roi-calculator/.env"
```

Сгенерировать новый токен:
```bash
openssl rand -hex 32
```

### Nginx-конфиг

Файл: `/etc/nginx/sites-available/ai-roi-calculator`

```nginx
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    client_max_body_size 10M;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 60s;
    }
}
```

### Скрипт обновления (`deploy.sh`)

Хранится на сервере: `/opt/ai-roi-calculator/deploy.sh`

```bash
#!/bin/bash
set -e
cd /opt/ai-roi-calculator
git pull origin main
npm ci
npx prisma generate
npx prisma db push
npm run build
pm2 restart roi
pm2 save
echo "Deploy complete"
```

Запуск с локальной машины:
```bash
ssh ai-roi-calculator "bash /opt/ai-roi-calculator/deploy.sh"
```

### Управление PM2

```bash
ssh ai-roi-calculator "pm2 status"           # статус всех процессов
ssh ai-roi-calculator "pm2 logs roi"         # логи (Ctrl+C для выхода)
ssh ai-roi-calculator "pm2 logs roi --lines 100"  # последние 100 строк логов
ssh ai-roi-calculator "pm2 restart roi"      # перезапуск приложения
ssh ai-roi-calculator "pm2 stop roi"         # остановка
ssh ai-roi-calculator "pm2 start roi"        # запуск
```

### Важно для продакшна
- `DATABASE_URL` должен быть абсолютным путём: `file:/opt/ai-roi-calculator/prisma/prod.db`
- `ADMIN_SECRET_TOKEN` — случайная строка (сгенерирована через `openssl rand -hex 32`)
- Prisma 6 требует явного `npx prisma generate` перед каждым билдом
- PM2 настроен на автозапуск при перезагрузке через `systemd` (`pm2 startup systemd`)

### Результат
✅ Приложение доступно по адресу **http://89.23.112.115**, работает стабильно через PM2 + Nginx.  
Админ-панель: `http://89.23.112.115/admin/{ADMIN_SECRET_TOKEN}`

---

## Фаза 8: Полировка

### Задачи
- [x] Пустые состояния (нет расчётов, нет шаблонов, нет данных на шаге результатов)
- [x] Drag & drop для изменения порядка этапов и ошибок (ProcessTable, ErrorTable)
- [x] Кнопка «Скопировать из AS-IS» на шагах TO-BE (процесс и ошибки)
- [x] Библиотека шаблонов: добавлен шаблон «Анализ откликов соискателей» (HR / Рекрутинг)
- [x] Библиотека шаблонов: добавлен шаблон «Расчёт себестоимости продукции» (Производство, 112 шагов AS-IS) через `prisma/seed.ts`
- [x] Сид базы данных: `prisma/seed.ts` + `npm run seed`; идемпотентный, работает локально и на сервере
- [x] Шаблоны: добавлено сохранение `timeHours` и `calendarDays` в `TemplateProcessStep`; при создании расчёта из шаблона время подставляется автоматически
- [x] Фикс: роут `GET /api/templates` переведён в `force-dynamic` (устранён баг с кэшированием Next.js)
- [x] Фикс: страница `/templates` переведена в `force-dynamic` — новые шаблоны появляются без пересборки
- [ ] Валидация ввода (отрицательные числа, пустые обязательные поля)
- [ ] Подсказки и tooltips для полей калькулятора
- [ ] Loading-состояния при навигации между расчётами
- [ ] Обработка ошибок API (toast-уведомления)
- [x] Мета-теги (title, description) — базовые
- [x] Favicon (из Next.js)
- [ ] Финальная проверка на планшете
- [ ] Оптимизация bundle size

### Результат
Продакшн-готовое приложение с хорошим UX.

---

---

## Фаза 9: ИИ-генерация данных

### Задачи
- [x] Создать OpenRouter-клиент (`src/lib/ai/openrouter.ts`)
  - Обёртка над `fetch` к `https://openrouter.ai/api/v1/chat/completions`
  - Retry при 429 (rate limit) — 1 повтор с задержкой 2 сек
  - Парсинг JSON из ответа (извлечение из markdown code-блоков)
  - Таймаут 60 секунд
- [x] Создать шаблоны промптов (`src/lib/ai/prompts.ts`)
  - Генерирующий промпт для каждой из 6 вкладок (process_asis, process_tobe, errors_asis, errors_tobe, capex, opex)
  - Верифицирующий промпт для каждой из 6 вкладок
  - Метаданные UI: заголовок диалога, placeholder, контекстная подсказка
  - Контекст (данные других вкладок) передаётся в текстовом виде внутри промпта
- [x] Создать серверный API-роут `POST /api/ai/generate`
  - Оркестрация цепочки: генерация (Gemini) → верификация (GPT)
  - Маппинг сырого JSON в типизированные объекты (`ProcessStep`, `ErrorItem`, `CapexItem`, `OpexItem`)
  - Валидация и нормализация числовых полей (зажим в допустимые диапазоны)
  - Ключ `OPENROUTER_API_KEY` используется только на сервере, клиент его не видит
  - Возвращает `503` если ключ не задан
- [x] Создать компонент `AiGenerateDialog` (`src/components/ai/ai-generate-dialog.tsx`)
  - Три стадии: ввод → генерация (loader) → превью результата
  - Readonly-превью таблицы с результатами (разный вид для каждого типа данных)
  - Блок `reasoning` — краткое пояснение от ИИ
  - Кнопки: «Применить» (заменить данные) / «Перегенерировать» / «Отмена»
  - Предупреждение о замене существующих данных
  - Ctrl/Cmd+Enter для быстрого запуска генерации
- [x] Интегрировать кнопку «Заполнить с ИИ» в `ProcessTable` (AS-IS и TO-BE)
- [x] Интегрировать кнопку «Заполнить с ИИ» в `ErrorTable` (AS-IS и TO-BE)
- [x] Интегрировать кнопку «Заполнить с ИИ» в `CapexTable` и `OpexTable`
- [x] Пробросить полный контекст расчёта из `CalculatorClient` в каждую таблицу
- [x] Добавить переменные окружения `OPENROUTER_API_KEY`, `AI_GENERATION_MODEL`, `AI_VERIFICATION_MODEL` в `.env.example`

### Результат
✅ На всех 6 вкладках ввода доступна кнопка «Заполнить с ИИ». Пользователь описывает процесс текстом — ИИ генерирует структурированные данные через двухшаговую цепочку (генерация → верификация), предлагает превью, применяет после подтверждения. Контекст уже заполненных данных (AS-IS шаги, ошибки, CAPEX и т.д.) автоматически учитывается в промптах.

### Технические детали
- Модели настраиваются через env-переменные без пересборки
- По умолчанию: генерация — `google/gemini-3.1-pro-preview`, верификация — `openai/gpt-oss-120b:free`
- API-роут объявлен с `export const runtime = "nodejs"` и `maxDuration = 120` (для Vercel Edge совместимости)
- Новых таблиц в БД не добавлялось — ИИ работает исключительно как генератор на лету
- Компонент `AiGenerateDialog` универсален для всех 6 типов вкладок через параметр `tabType`

### Важно для деплоя
После выкатки на продакшн добавить в `.env` на сервере:
```bash
ssh ai-roi-calculator "echo 'OPENROUTER_API_KEY=sk-or-v1-...' >> /opt/ai-roi-calculator/.env"
ssh ai-roi-calculator "echo 'AI_GENERATION_MODEL=google/gemini-3.1-pro-preview' >> /opt/ai-roi-calculator/.env"
ssh ai-roi-calculator "echo 'AI_VERIFICATION_MODEL=openai/gpt-oss-120b:free' >> /opt/ai-roi-calculator/.env"
ssh ai-roi-calculator "pm2 restart roi"
```

---

## Известные технические особенности

### Prisma 6
- Генератор `prisma-client` (не `prisma-client-js`) — выход в `src/generated/prisma/`
- Импорт клиента: `import { PrismaClient } from "@/generated/prisma/client"`
- Конфигурация через `prisma.config.ts` (загружает `.env` через dotenv)
- `DATABASE_URL` должен быть **абсолютным путём** для корректной работы в Next.js dev

### Next.js App Router
- Динамический сегмент `[id]` в `src/app/[id]/` — страница калькулятора
- API-роуты в `src/app/api/` — route handlers (GET, POST, PUT, DELETE)
- `POST /api/calculations` возвращает `303 See Other` для redirect после создания
- Страницы с данными из БД (`/templates`) требуют `export const dynamic = "force-dynamic"` — иначе Next.js кэширует их при сборке и новые записи не отображаются

### Сид базы данных
- Скрипт: `prisma/seed.ts`, запуск: `npm run seed`
- Использует `tsx` (devDependency) для выполнения TypeScript без предкомпиляции
- Загружает `.env` через `dotenv` (`config({ path: resolve(__dirname, "../.env") })`)
- Идемпотентен: перед созданием удаляет шаблон с тем же именем — безопасно запускать повторно
- На сервере запускать отдельно после `deploy.sh`: `ssh ai-roi-calculator "cd /opt/ai-roi-calculator && npm run seed"`

---

### OpenRouter API
- Клиент: `src/lib/ai/openrouter.ts` — единственное место, откуда происходят обращения к API
- Ключ читается из `process.env.OPENROUTER_API_KEY` — доступен только серверному коду
- JSON из ответа модели может быть обёрнут в markdown code-блок — клиент умеет его распаковывать
- Параметр `response_format: { type: "json_object" }` передаётся в запрос — модели, поддерживающие structured output, возвращают чистый JSON
- Промпты хранятся в `src/lib/ai/prompts.ts`; при добавлении новой вкладки достаточно добавить case в `buildGenerationMessages` и `buildVerificationMessages`

---

---

## Фаза 10: Авторизация и переделка шаблонов

### Задачи

- [x] Обновить Prisma schema:
  - Добавить модель `User` (id, email, passwordHash, role, timestamps)
  - Расширить `Calculation` полями: userId, isTemplate, description, industry, isPublished
  - Удалить Template-модели (Template, TemplateProcessStep, TemplateErrorItem, TemplateCapexItem, TemplateOpexItem, TemplateRolloutConfig)
  - Выполнить `prisma db push --accept-data-loss`
- [x] Установить зависимости: `bcryptjs`, `@types/bcryptjs`, `jose`
- [x] Создать `src/lib/auth.ts` — хелперы: hashPassword, verifyPassword, createToken, verifyToken, setAuthCookie, clearAuthCookie, getCurrentUser, getUserFromRequest, requireAuth, requireAdmin, isAdminEmail
- [x] API-роуты авторизации:
  - `POST /api/auth/register` — регистрация (auto-ADMIN по ADMIN_EMAILS)
  - `POST /api/auth/login`
  - `POST /api/auth/logout`
  - `GET /api/auth/me`
- [x] `AuthProvider` React context (`src/components/auth/auth-provider.tsx`)
- [x] Страница `/login` — форма с табами «Войти» / «Зарегистрироваться»
- [x] Обновить `AppHeader` — кнопка «Войти» / email + меню выхода + ссылка «Управление шаблонами» для ADMIN
- [x] Обновить `layout.tsx` — добавить `<AuthProvider>`
- [x] `POST /api/calculations` — записывать userId при создании если пользователь авторизован
- [x] `GET /api/calculations/my` — расчёты текущего пользователя
- [x] Обновить `MyCalculationsClient` — авторизованные получают данные с сервера
- [x] Обновить `CalculatorClient` — не сохранять ID в localStorage если авторизован; показывать кнопку «Сохранить как шаблон» для ADMIN
- [x] `POST /api/templates/from-calculation` — создание шаблона из расчёта (все данные включая стоимости)
- [x] `SaveAsTemplateDialog` — диалог с выбором «новый / заменить существующий»
- [x] Обновить `GET /api/templates` — использовать Calculation вместо Template; параметр `?all=1` для ADMIN
- [x] Обновить `GET /api/templates/:id` и `POST /api/templates/:id/use`
- [x] `PATCH/DELETE /api/templates/manage` — публикация / удаление для ADMIN
- [x] Страница `/templates/manage` — управление шаблонами (только ADMIN)
- [x] Обновить `GET /templates` page — использовать Calculation.findMany
- [x] Удалить старую админку: `src/app/admin/`, `src/app/api/admin/`, `src/components/admin/`
- [x] Обновить `prisma/seed.ts` — создавать шаблоны как Calculation с isTemplate=true
- [x] Обновить `.env.example` — JWT_SECRET, ADMIN_EMAILS (убрать ADMIN_SECRET_TOKEN)
- [x] Обновить `README.md`, `PRODUCT_SPEC.md`, `DATA_MODEL.md`, `DEVELOPMENT_PLAN.md`

### Результат
✅ Авторизация через email + пароль. Расчёты авторизованных пользователей доступны с любого устройства. Администратор создаёт шаблоны кнопкой «Сохранить как шаблон» прямо из расчёта — с полными данными. Старая token-based админ-панель удалена.

### Технические детали
- JWT хранится в httpOnly-cookie, срок жизни 30 дней, подписан `JWT_SECRET`
- Роль ADMIN назначается при регистрации по env-списку `ADMIN_EMAILS`
- Шаблон = Calculation с `isTemplate = true` (6 отдельных Template-моделей удалены)
- Анонимный доступ сохранён: расчёты без userId работают как прежде

### Важно для деплоя
После выкатки на продакшн:
```bash
# 1. Добавить новые переменные в .env на сервере
ssh ai-roi-calculator "echo 'JWT_SECRET=$(openssl rand -hex 32)' >> /opt/ai-roi-calculator/.env"
ssh ai-roi-calculator "echo 'ADMIN_EMAILS=your@email.com' >> /opt/ai-roi-calculator/.env"
# Удалить старый ADMIN_SECRET_TOKEN из .env на сервере

# 2. Запустить deploy.sh (включает prisma db push --accept-data-loss)
ssh ai-roi-calculator "bash /opt/ai-roi-calculator/deploy.sh"

# 3. Зарегистрироваться на сайте с email из ADMIN_EMAILS — получить роль ADMIN
```

---

## Будущие улучшения (бэклог)

- [ ] Экспорт в PDF
- [ ] Экспорт в Excel
- [ ] Мультиязычность
- [ ] OAuth авторизация
- [ ] Командная работа (совместное редактирование)
- [ ] Мобильная адаптация
- [ ] Домен и SSL (Let's Encrypt)
- [ ] Аналитика использования
- [ ] Тесты для модуля расчётов (`src/lib/calculations/`)
- [ ] Дублирование расчёта
- [ ] Streaming-режим для ИИ-генерации (отображать этапы по мере генерации)
- [ ] История генераций в рамках одного расчёта (возможность откатиться)
