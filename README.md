# AI ROI Calculator

Веб-сервис для расчёта ROI внедрения ИИ-решений в бизнес-процессы.

Позволяет описать процесс «как есть» (AS-IS) и целевое состояние с ИИ (TO-BE), учесть ошибки, динамику раскатки, OPEX/CAPEX и получить наглядный расчёт окупаемости.

## Статус проекта

**MVP задеплоен.** Фазы 1–7 реализованы. Проект доступен по адресу: **http://89.23.112.115**

| Фаза | Статус |
|------|--------|
| 1: Инициализация | ✅ Завершена |
| 2: Модель данных и API | ✅ Завершена |
| 3: Калькулятор — ввод данных | ✅ Завершена |
| 4: Расчёты и графики | ✅ Завершена |
| 5: Библиотека и история | ✅ Завершена |
| 6: Админ-панель | ✅ Завершена |
| 7: Деплой на Timeweb | ✅ Завершена |
| 8: Полировка | 🔄 Частично |

## Технологический стек

| Слой | Технология | Обоснование |
|------|-----------|-------------|
| Frontend + Backend | **Next.js 14** (App Router, TypeScript) | Единый проект, SSR, API Routes — минимум конфигурации |
| UI-компоненты | **shadcn/ui** + **Tailwind CSS** | Красивые готовые компоненты, быстрая стилизация |
| Графики | **Recharts** | React-нативная библиотека, кастомизируемая, красивая |
| ORM | **Prisma 6** | Типобезопасность, миграции, простота |
| База данных | **SQLite** | Лёгкая, без отдельного сервера, достаточна для проекта |
| Деплой | **Timeweb VPS** | Бюджетный VPS, доступ по IP |

## Структура проекта

```
ai_roi_calculate/
├── .cursor/
│   └── rules/              # Правила для AI-агентов Cursor
├── docs/
│   ├── PRODUCT_SPEC.md     # Продуктовая спецификация
│   ├── DEVELOPMENT_PLAN.md # Поэтапный план разработки (с актуальным статусом)
│   └── DATA_MODEL.md       # Модель данных
├── prisma/
│   ├── schema.prisma       # Схема БД (12 моделей)
│   └── dev.db              # SQLite база (не в git)
├── prisma.config.ts        # Конфигурация Prisma 6
├── src/
│   ├── app/                # Next.js App Router
│   │   ├── [id]/           # Страница калькулятора
│   │   ├── admin/[token]/  # Админ-панель
│   │   ├── my/             # Мои расчёты
│   │   ├── templates/      # Библиотека шаблонов
│   │   ├── api/            # API Route handlers
│   │   │   ├── calculations/
│   │   │   ├── templates/
│   │   │   └── admin/
│   │   ├── layout.tsx
│   │   └── page.tsx        # Главная страница
│   ├── components/
│   │   ├── ui/             # shadcn/ui + DecimalInput (дробные числа)
│   │   ├── process/        # ProcessTable (AS-IS / TO-BE)
│   │   ├── errors/         # ErrorTable
│   │   ├── costs/          # CapexTable, OpexTable, RolloutForm
│   │   ├── charts/         # ResultsPanel, RoiChart, MonthlySavingsChart, ...
│   │   ├── layout/         # AppHeader
│   │   ├── admin/          # AdminClient, AdminTemplateForm
│   │   ├── calculator-client.tsx  # Главный компонент калькулятора
│   │   ├── templates-client.tsx
│   │   └── my-calculations-client.tsx
│   ├── lib/
│   │   ├── calculations/   # Чистые функции расчётов
│   │   │   ├── process-savings.ts
│   │   │   ├── error-savings.ts
│   │   │   ├── rollout.ts
│   │   │   └── roi.ts
│   │   ├── db.ts           # Prisma client singleton
│   │   └── format.ts       # formatMoney, formatNumber, formatPercent, formatMultiplier
│   ├── generated/prisma/   # Сгенерированный Prisma client (не в git)
│   └── types/
│       └── index.ts        # Все TypeScript типы
├── .env                    # Секреты (не в git)
├── .env.example            # Пример переменных окружения
├── package.json
└── tsconfig.json
```

## Быстрый старт (разработка)

```bash
# 1. Установка зависимостей
npm install

# 2. Настроить переменные окружения
cp .env.example .env
# Отредактировать .env: задать абсолютный путь DATABASE_URL и ADMIN_SECRET_TOKEN

# 3. Инициализация БД
npx prisma db push

# 4. Запуск dev-сервера
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

### Важно: DATABASE_URL

Для корректной работы в dev и prod DATABASE_URL должен быть **абсолютным путём**:

```env
DATABASE_URL="file:/абсолютный/путь/к/проекту/prisma/dev.db"
ADMIN_SECRET_TOKEN="ваш-секретный-токен"
```

### Доступ к админ-панели

```
http://localhost:3000/admin/{ADMIN_SECRET_TOKEN}
```

## API-роуты

| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/calculations` | Создать расчёт → redirect на `/{id}` |
| GET | `/api/calculations/:id` | Получить расчёт со всеми связями |
| PUT | `/api/calculations/:id` | Обновить весь расчёт (транзакция) |
| DELETE | `/api/calculations/:id` | Удалить расчёт |
| GET | `/api/templates` | Список опубликованных шаблонов |
| GET | `/api/templates/:id` | Получить шаблон |
| POST | `/api/templates/:id/use` | Создать расчёт из шаблона → redirect |
| GET/POST | `/api/admin/templates` | Список/создание шаблонов (токен) |
| GET/PUT/DELETE | `/api/admin/templates/:id` | CRUD шаблона (токен) |

## Правила разработки

### Общие принципы
1. **Язык кода** — английский (переменные, функции, комментарии)
2. **Язык интерфейса** — русский
3. **TypeScript** — strict mode, никаких `any` без крайней необходимости
4. **Компоненты** — функциональные, с хуками, без классов
5. **Стили** — только Tailwind CSS, никакого inline CSS и CSS-модулей

### Архитектурные правила
1. Бизнес-логика расчётов живёт в `src/lib/calculations/` — чистые функции
2. Компоненты не содержат бизнес-логику — только отображение и обработка ввода
3. API-роуты в `src/app/api/` — тонкие, делегируют логику в `src/lib/`
4. Все типы данных описаны в `src/types/index.ts`
5. Prisma-клиент инициализируется один раз в `src/lib/db.ts`
6. Вычисляемые поля (`stepCost`, `unitTime` и т.д.) — только на клиенте, не хранятся в БД

### Работа с данными
1. Расчёт работает без авторизации — данные привязаны к уникальному UUID
2. История расчётов хранится в localStorage (список ID)
3. SQLite-файл: `prisma/dev.db` (dev), `prisma/prod.db` (prod)

### Именование
- Компоненты: `PascalCase` (экспорт), файлы: `kebab-case.tsx`
- Утилиты/хелперы: `camelCase`
- Типы/интерфейсы: `PascalCase`, без префикса `I`
- API-роуты: `kebab-case` в URL

### Git
- Коммиты на русском, формат: `тип: описание`
- Типы: `feat`, `fix`, `refactor`, `docs`, `style`, `chore`
- Ветка `main` — стабильный код

## Продакшн-сервер

| Параметр | Значение |
|----------|----------|
| Хостинг | Timeweb VPS, ru-1 (Санкт-Петербург) |
| IP | `89.23.112.115` |
| Тариф | 1 vCPU / 2 GB RAM / 30 GB NVMe (~550 руб/мес) |
| ОС | Ubuntu 22.04 LTS |
| Node.js | v20.20.0 LTS |
| Процесс | PM2 (процесс `roi`, автозапуск при перезагрузке) |
| Прокси | Nginx → localhost:3000 |
| БД | SQLite: `/opt/ai-roi-calculator/prisma/prod.db` |
| Репозиторий | `/opt/ai-roi-calculator` |

### Доступ

```bash
# SSH
ssh ai-roi-calculator
# или явно:
ssh -i ~/.ssh/id_ed25519_timeweb root@89.23.112.115

# Приложение
http://89.23.112.115

# Админ-панель (токен хранится в /opt/ai-roi-calculator/.env на сервере)
http://89.23.112.115/admin/{ADMIN_SECRET_TOKEN}
```

### Обновление приложения

```bash
# Запустить скрипт деплоя на сервере:
ssh ai-roi-calculator "bash /opt/ai-roi-calculator/deploy.sh"
```

Скрипт выполняет: `git pull` → `npm ci` → `prisma generate` → `prisma db push` → `npm run build` → `pm2 restart roi`.

### Управление процессом

```bash
ssh ai-roi-calculator "pm2 status"          # статус
ssh ai-roi-calculator "pm2 logs roi"        # логи в реальном времени
ssh ai-roi-calculator "pm2 restart roi"     # перезапуск
```

Подробная инструкция по первоначальной настройке — в `docs/DEVELOPMENT_PLAN.md`, фаза 7.

## Документация

| Документ | Описание |
|----------|----------|
| [Продуктовая спецификация](docs/PRODUCT_SPEC.md) | Полное описание функционала, ролей, экранов |
| [План разработки](docs/DEVELOPMENT_PLAN.md) | Фазы, задачи, текущий статус |
| [Модель данных](docs/DATA_MODEL.md) | Схема БД, связи, типы |
